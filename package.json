{
  "name": "snafleshub-official",
  "version": "1.0.0",
  "private": true,
  "description": "Production deployment wrapper for SnaflesHub",
  "scripts": {
    "postinstall": "npm --prefix server ci --omit=dev && npm --prefix client ci && npm --prefix client run build",
    "build": "npm --prefix client run build",
    "start": "node startup.js"
  },
  "engines": {
    "node": "24.x"
  }
}
